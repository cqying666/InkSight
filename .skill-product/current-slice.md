# 产品交付简报

## 目标

- 用户：使用 InkSight 拆文、积累素材并创作短篇小说的创作者。
- 核心问题：仪表盘、拆文报告、素材库与创作工作台之间数据断裂，用户无法完成从学习到创作的闭环。
- 成功信号：用户可在一次连续流程中完成拆文、选择素材入库、新建素材、在工作台引用文档与素材咨询 AI、编辑并保存正文，首页同步展示闭环进度。

## 范围

- 范围内：首页闭环区、拆文候选素材适配与入库、素材新建、五类工作台文档、AI 多源引用、编辑器基础能力和埋点口径修正。
- 范围外：账号系统、云数据库、跨设备同步、多人协作、模型供应商更换、拆文提示词重写。
- 约束：保留现有本地存储键与已有数据；不自动沉淀；不把演示报告写入个人素材库。

## 用户流程

1。入口：首页可直接上传拆文或进入创作，并查看自己的创作闭环。
2。核心动作：上传小说并完成分析，在报告中选择候选素材和文件夹后确认入库。
3。延续动作：在素材库新建素材，进入工作台编辑五类文档，并为 AI 勾选文档或素材引用。
4。成功状态：素材刷新后仍在正确分类；文档刷新后仍保留；AI 请求显示已选引用；正文可保存、标记完成并触发写后分析。
5。失败状态：演示报告禁止入库；空候选提示；AI 缺少配置或超时时保留用户输入与引用；本地保存失败明确提示。

## 最小交付切片

- 切片名称：InkSight 本地单用户创作闭环。
- 主责：World－Class Product Architect。
- 工程协同：Technical Trinity。
- 依赖：现有 AnalysisResult、Material、AnalyticsEvent、DraftData 与 WriteOutline 契约。
- 恢复锚点：`.skill-product/current-slice.md`。

## 增量切片：例文库与原文回看

- 目标：把素材库首项从混合素材列表改为独立例文入口，统一承接拆文上传和仅保存原文两类作品。
- 数据契约：例文与素材分表存储；列表接口只返回书名、导语前三句、题材、标签和拆文状态，详情接口按编号读取完整原文与可选报告。
- 用户流程：上传例文后立即显示卡片；仅上传作品进入纯原文页；已拆文作品进入 X 光报告的原文标签；纯原文页可继续发起拆文并升级同一记录。
- 失败约束：例文写入失败不得关闭上传弹窗或伪装成功；已拆文记录缺少报告时不得回退到演示报告。
- 验证证据：浏览器完成例文上传、三句导语卡片、题材标签、纯原文跳转和 X 光原文跳转验证；测试记录已清理。

## 增量切片：首页自然语言意图路由与导语二创

- 目标用户：在首页一个输入框内提交指令和小说内容、期望系统自动选择正确创作流程的作者。
- 核心问题：首页目前用关键词和总长度粗略分流，无法可靠区分全文拆文、纯导语分析、导语分析加二创、导语生成，也没有把指令与待分析文本分离。
- 成功信号：相同首页入口能稳定路由五类代表输入；导语二创报告同时呈现导语分析、核心元素、三至五个差异化二创方向与推荐方向。
- 范围内：纯函数意图路由、首页接入、导语分析输入模式、导语二创 Schema/Prompt、结果页展示和合同测试。
- 范围外：通用聊天 Agent、完整拆文 Schema 重写、模型供应商切换、自动写入作品。
- 主责：World-Class Product Architect；工程实现：Technical Trinity。
- 恢复锚点：`.skill-product/current-slice.md`。

### 交付结果

- 首页新增确定性意图路由与指令/正文切分，支持冒号、段落、首行和中文引号包裹正文。
- `/api/guide-analysis` 兼容 `analysis` 与 `analysis_with_directions` 两种模式。
- 二创模式新增可迁移核心元素、3—5 条方向、推荐方向和避雷；纯分析模式不返回方向。
- 合同测试、TypeScript 和生产构建通过；真实二创请求返回 5 条方向，真实纯分析请求返回 0 条方向。
- 本地 production preview 已用最终构建重启并运行于 `http://localhost:3000`。
