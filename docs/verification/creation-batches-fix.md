# 素材分析、候选批次与跨批差异修复

日期：2026-09-18；本次用户明确要求修复，不代表整套Token压缩实施获批。

## 变更

- 从实际生成消息恢复候选组及顺序，选择消息不会被误认成新候选组。
- 明确的“继续聊第二个”绑定最新组第二个，保留原selected，只更新focused；明确最初/上一组按相应组解析，越界需澄清。
- 同素材历史候选结构摘要同时送入生成和检查；精确复刻及已观察到的“换身份后仍撤资源使生意垮掉”模式由代码拦截，其余结构差异由带对照证据的模型检查验证。
- 新批次需提供完整新旧对照：新候选序号、最相似历史ID、是否共用因果骨架，以及具体结构差异。通过的对照记录与候选消息一起保存。
- 素材生成同一次调用输出共享分析，页面可展开人物事件、冲突、情绪及潜在吸引力。没有为每个候选增加单独分析调用。

## 验证

- creation回归、候选分组/指代和跨批结构检查测试通过。
- TypeScript及隔离生产构建通过，保留既有依赖/字体/编辑器hooks警告。
- 真实模型测试使用临时独立数据库，读取原失败会话作为复现资料，不修改原会话。
- 首次新旧对照模型放过了“隐藏供货身份、停止供货让对方垮掉”的同骨架方案，人工复核未将其认定成功；随后补充窄范围结构拦截和反事实检查。旧结果见 creation-batches-live-before-structural-guard.json。
- 期间遇到模型JSON格式失败，保留记录于 creation-batches-live-json-failure.json，未保存失败候选。
- 新婚素材的分析已生成并保留人物事实与未知信息、情绪变化及五项潜在吸引力，民俗按角色观念解释；不把“爆款元素”等同结果保证。
- 最终实时复测与页面状态在本轮完成后补充。

## 边界

这不是通用文学原创性检测。窄规则只覆盖已观察到的重复模式，语义差异仍依赖模型与作者判断；通过自动检查不代表必然有新意或能卖。

本次补齐的是候选分组、明确指代及跨批对照，不能宣称短期记忆机制已全面完善。自然语言混合指令与复杂歧义仍由现有任务识别处理；自动Token压缩仍未上线。
