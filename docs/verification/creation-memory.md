# 会话工作记忆与上下文组织验收

日期：2026-09-14。实现说明见 `docs/plan/creation-agent-working-memory.md`。

## 已交付

- 当前会话完整对象索引；模型按目标选择相关报告、方向和讨论记录，后续工具使用统一 context。
- 选定和聚焦版本明确区分并固定带入；早期候选的来源报告与后续相关分析可同时使用。
- 作者决定逐字溯源、按方向家族限定、显式替代旧决定；讨论建议需要作者明确采纳。
- 历史未结构化答复可作为 proposal 索引恢复，不能自动变为设定。
- 每轮实际输入的上下文清单持久化，页面可展开查看。
- 综合讨论不必生成新方向；非阻塞的反馈邀请不把已完成讨论标成等待。

## 确定性验证

`npm run test:creation` 通过，包括新增 `tests/creation-memory.mts`：

- 7 份报告及 15 个候选下，早期选定版本与指定章节报告均被保留，无关报告不因较新而被采用。
- 规划、方向调整与检查模型均获得原始导语报告和后续章节报告。
- 方向切换时专属决定不串用；明确采纳与旧决定替代可追溯。
- 同会话隔离、不存在/跨用户对象拒绝、预算超限与刷新恢复。
- “给出针对已选方向的具体修改建议，暂不生成新方向”正常完成且不新增方向。
- 既有分析范围、引文、知识、版本、停止隔离和流式传输回归通过。

`npx tsc --noEmit --incremental false` 通过。

`INKSIGHT_NEXT_DIST_DIR=.next-creation-memory-check npm run build` 通过。保留已有 Pi 动态依赖、字体及编辑器 hooks 构建警告。

## 真实模型验证

使用原会话内容和原模型配置，在临时独立数据库复测，并加入 4 份无关科幻报告作为干扰。脚本 `scripts/verify-creation-memory.mts` 需显式设置会话 ID 才调用模型。

- 最终 `creation-memory-live.json`：完成；相关导语/章节报告均被读取，无干扰报告，不新增方向，选定版本保留，作者“不靠男二”要求被记录。
- 保留两次调试证据：`creation-memory-failed-completion.json`（旧关键词完成条件把讨论误判为生成，导致决策耗尽），`creation-memory-waiting-review.json`（已有综合答复但错误地标为等待，并将选定设定降格成未采纳候选）。修正相应规则后复测通过。

## 原页面验证

用户原 Chrome 标签页为 localhost:3107，已更新此入口；3000 同步使用相同构建。

在原会话发送“结合导语背景/冲突/钩子及第一章大概念，讨论已选定寿宴 v1 的结构功能和具体修改建议，暂不生成新方向，保持女主主动性且不靠男二”。

- 页面显示“本轮完成”，未新增方向，候选数保持 5。
- 答复实际对齐了导语、第一章报告及已选定寿宴 v1。
- 展开“本轮上下文”可见 4 份分析、1 条作者决定；v1 标为当时已选定，v3 标为当时正在讨论。
- 数据库检查与页面一致；selected 与 focused 均保持原值，新答复保存了 context，作者要求保存为 active 决定，讨论结果保存为 proposal。
- 本轮 reference/target 清单同时包含 v1 与 v3，回复依明确的 v1 要求展开；不把这一例解释为所有多版本指代都能无误消解。

实际记录：`creation-memory-browser.json`。

## 当前边界与后续

- 本次是当前会话内的公共底座；完整单章情节规划和跨会话长期记忆没有宣称交付。
- 上下文选择和明确决定识别仍依赖模型。本次样例成功不代表稳定文学质量通过；模型生成建议仍可能有不合适的剧情或表述，需要作者判断。
- 目前报告原文完整传入且可能有重复相关报告；大上下文会明确触发预算限制。后续可增加可追溯摘要、重复报告收敛和更精确的 token 分配。
- 逐字来源、对象 ID、版本和范围校验提供可查证据，不等于自动证明语义正确。
