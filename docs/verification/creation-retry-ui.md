# 素材生成重试状态展示修复

日期：2026-09-16。用户仅授权实施交互问题；框架多样性、全量知识接入、自动同步与检索升级尚在讨论，本次均未修改。

- 同轮素材提炼/素材修订动作先失败、后成功时，失败条目显示“首次失败，后续重试成功”，原始错误折叠保留。
- 没有后续成功仍显示失败；不以整轮完成、其他动作成功或先前成功冒充恢复。
- 仅前端派生展示，不改历史执行记录、生成逻辑、知识或数据库。
- 专项测试、类型检查、隔离生产构建通过。直接读取用户小店会话的真实步骤验证，恢复条目匹配1个。
- 3117已切换到 `.next-retry-ui` 最终构建，HTTP 200；3000/3107未改。
- 测试已并入 `npm run test:creation`。
- 启动：`INKSIGHT_NEXT_DIST_DIR=.next-retry-ui npm run start -- --port 3117`。
